import { createTheme } from "@mui/material";

const theme = createTheme({
	direction: "rtl",
	typography: {
		fontFamily: "iranyekan",
		fontSize: 13,
		button: {
			textTransform: "none",
		},
	},
});

export default theme;
